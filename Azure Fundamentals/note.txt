Azure vm=Ec2

Shared Responsibilty model -
the cloud is responsible for security,physical computer security 
your responsible for whatever runs on that vm or the servies they provide 
like os,network and firewall settings,authencation,data and apps 

Azure local - it tool that lets to handle your local private servers

Public cloud-
The public cloud is defined as computing services offered by third-party
providers over the public Internet, making them available to anyone who wants 
to use or purchase them

Private Cloud-
The private cloud is defined as computing services offered either over the 
Internet or a private internal network and only to select users instead of
the general public

Hybrid cloud-
A hybrid cloud is a computing environment that combines a private cloud
with a public cloud
advantage is has the public cost and efficiency and private infrastrucutre 


Inbound traffic is free and outbound traffic is billed in db

High Availability (HA)-
ablity of a system to remain opertaional to users during outages also.

Scalablity-
the ablity of a system to accommodate increasing demand by adding or removing
resources as needed.	

Vertical Scaling -scaling up or down
Horizontal Scaling -Scaling out or in 

Elasticity-
The abilty of a system to quickly and easily scale up or down the resources
that a system uses in response to chaging demand 

IAAS-
Infrastructure as a service
they provide computing,stroage,networking services

Paas-
Platform as a service
they provideservice layes on top of iaas 
ex-azure app services,managed Stroage.azure front door

Saas-
Software as a service 
they provide tools like office 35,onedrive,skype

Serverless-
you dont managae the servers write the code and upload it
azure functions =lambda 
container apps,kubernetes,sql db,cosmos db they are serverless

there are 60 regions in total  

Zonal services-
you can choose a specific az to delpoy the service 
you can also choose another duplicate service and deploy in another az 
to achive resilisency

Zone-redundant service-
it automatically deploys across zones for you no need to configure it 
ex- azure db 

always available service-
these are gloabal services and microsoft will take care that it will
always run 
ex-non regional service

App Services (Web apps)-
you package your code and upload it and give the configuration to run it 
no access to hardware this is paas
used to host web sites,restful apis,mobile backend


Conatiner services-
1) azure container instace-single instace quickest way to delpoy
2) azure container apps -easy to use like web service with advanced features
3) azure kubernetes services - runs on a cluster of servers, entripse -grade 

Azure Virtual Desktop-
desktop version of windos that runs in cloud 

vnets= vps 
nsg-network security group

peering-this allows communication between a vm on one network and another
vm on a different network 

Azure dns-
you can give your ip addresses name using dns 
it only applies internally to azure to applied networks 

Azure Stroage (GPv2)-
they can store 4 types of data-container,file,queue,table
max 5pb 
cheap cloud stroage
not recommened for high-deamand workloads
2 cents per gb per month 

Data Lake-
extermely large storage 
for big data 

Blob stroage- binary large object = s3 bucket
unstructued data
they can store any type of data 
stored lossely in container
blob can be part of only one container

Access tiers -
i)hot-the default,balanced access
ii)cool-can be set as default , cheaper storage with more expensive read/wite
operations 
iii)cold-much cheaper storage,more expensive r/w operations 
iv)archive-cannot get immediate access to files ,cheapest storage,most 
expensive operations

LRS &ZRS - 3copies 
LRS copies data 3 times within one datacenter. ZRS copies data 3 times across 
3 separate Availability Zones within a region.

GRS & GZRS- 6 copies
grs -Replicated to 3 zones using LRS 
gzrs-Replicated to 3 zones using ZRS

Azcopy-
copying data from on-premises source to azure storage
and vice-versa  or azure stroage to azure storage 

SAS tokens-
they give access to blob stroage such r/w/x

we need both the sas token for source and destination
and use azcopy command
azcopy copy "source_path" "https://[account].blob.core.windows.net/[container]?[SAS_token]"

Azure migrate -
used migrate on-premises server into azure 

azure data box = snowball or snowmobile 
data box-
they send the data box based hard disk on your data size and after copying 
you send it back it is encrypted while traveling 

entra = IAM    

Authentication -it is a user porving who they are using user id 
and password

Authorization -it is way of ensuring that a user is permitted to perform
an action

External identities-
the ability to give access to your azure resources to people outside your
organiztion 

Passwordless login-
using iris,face and fingerprint 

Role-based control (RBAC)-
it is a method of restricting access to the resources based on roles in the 
organiztion

JIT - Just-in-time 
JEA -just-enough-access -
this increases the time of sso just enough until the user becomes non-elevated user 	

Cost-Mangement in Azure -
Factors that affect the cost -
1)resource type 
2)usage and billing model 
pay as you go,reserved instances,spot pricing 
3)service tiers and configuration 
for ex in app service free basic standard and premium
chosing more cpu,memory or performance 
4)Licensing cost 
windows or sql licese
5)Data Transfer
inbound data is free and outband data is charged beyond certain limit
6)unused resource
vm will be charged still if not used 
7)addtitional considerations-
storage access tiers like hot cool archive 

Azure Policy enforces rules on resource properties to ensure compliance,
while Azure Blueprints (being replaced by template specs) orchestrate the 
deployment of ARM templates, policies, and role assignments to create compliant 
environments

Resource lock-
it is a way to restrict the abilty to modify or delete that resource
prevent accidents or any attacks 

two types -
1)read only 2) can not delete
Scope-
1)resource level 2)resource group level 3)subscription level 
even the owner will not be able to access if lock is implemented it 

Azure Arc uses the dashboard of azure to control vms and containers
across various other clouds also 

Infrastructure as a code (IAC)-
this saves your configuration in a file
for this we use arm templates,bicep,terraform,chef,puppet,powershell
scripts  

ARM-Azure Resource Manager -
Azure Resource Manager (ARM) is the native management and deployment 
layer for Azure, allowing you to create, update, and delete resources 
(VMs, databases, web apps) in a consistent, repeatable way.

Monitoring tools-
Azure advisor
azure service health

Kusto Query Language (KQL)-
it is used in azure monitor reports there are pre-defined queries 
